name: tests

on:
  push:
    branches: ["**"]
    paths:
      - backend/**
      - pyproject.toml
      - poetry.lock
      - alembic.ini
      - pytest.ini
      - .coveragerc
      - .flake8
      - mypy.ini
      - .github/workflows/tests.yml
  pull_request:
    paths:
      - backend/**
      - pyproject.toml
      - poetry.lock
      - alembic.ini
      - pytest.ini
      - .coveragerc
      - .flake8
      - mypy.ini
      - .github/workflows/tests.yml
  workflow_dispatch:
    inputs:
      deploy:
        type: boolean
        required: false
        default: false

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pytest:
    runs-on: [self-hosted, linux]
    timeout-minutes: 20

    env:
      PYTHON: /opt/ispmanager/python3.12/bin/python3.12

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Python
        run: |
          test -x "$PYTHON" || (echo "Python not found/executable at $PYTHON" && ls -la "$(dirname "$PYTHON")" && exit 1)
          "$PYTHON" --version
          "$PYTHON" -c "import sys; assert sys.version_info[:2] == (3, 12), sys.version"

      - name: Install Poetry
        run: |
          "$PYTHON" -m pip install --upgrade pip
          "$PYTHON" -m pip install "poetry==1.8.2"

      - name: Configure Poetry
        run: |
          "$PYTHON" -m poetry config virtualenvs.in-project true
          "$PYTHON" -m poetry --version
          "$PYTHON" -m poetry env use "$PYTHON"

      - name: Install dependencies
        run: |
          "$PYTHON" -m poetry install --with dev

      - name: Lint (flake8)
        run: |
          "$PYTHON" -m poetry run flake8

      - name: Typecheck (mypy)
        run: |
          "$PYTHON" -m poetry run mypy backend

      - name: Run tests
        run: |
          "$PYTHON" -m poetry run pytest

  deploy_backend:
    needs: pytest
    runs-on: [self-hosted, linux]
    timeout-minutes: 20
    environment: hsepoker-prod
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/main')
      || (github.event_name == 'workflow_dispatch' && inputs.deploy == true)

    env:
      DEPLOY_PATH: /var/www/www-root/data/www/hsepoker.ru

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy backend
        run: |
          mkdir -p "$DEPLOY_PATH"
          command -v rsync >/dev/null 2>&1 || (echo "rsync not found" && exit 1)
          test -w "$DEPLOY_PATH" || (echo "No write permission for $DEPLOY_PATH" && ls -la "$DEPLOY_PATH" && exit 1)
          rsync -a --delete \
            --exclude '__pycache__/' \
            --exclude '*.pyc' \
            --exclude '.pytest_cache/' \
            --exclude '.mypy_cache/' \
            --exclude '.venv/' \
            backend/ "$DEPLOY_PATH/backend/"
          rsync -a \
            alembic.ini \
            pyproject.toml \
            poetry.lock \
            docker-compose.yml \
            pytest.ini \
            .coveragerc \
            .flake8 \
            mypy.ini \
            "$DEPLOY_PATH/"
